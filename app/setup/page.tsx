import { SetupClient } from "./setup-client"

export const metadata = {
  title: "Configurar Dispositivo | PR Delivery",
  description: "Configura tu tablet para acceder al KDS",
}

export default function SetupPage() {
  return <SetupClient />
}
